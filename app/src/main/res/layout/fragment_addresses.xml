<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout android:background="@color/white"
    android:padding="@dimen/_16sdp"
    android:id="@+id/content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:visibility="gone"
        android:layout_gravity="center"
        android:gravity="center"
        android:drawablePadding="@dimen/_16sdp"
        android:id="@+id/card_empty_address"
        android:padding="@dimen/_32sdp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_16sdp"
        app:drawableTopCompat="@drawable/ic_addresses_empty"
        android:text="@string/no_addresses_found" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rec_addresses"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/item_address" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>