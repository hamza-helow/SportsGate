<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="product"
            type="com.souqApp.domain.main.home.entity.ProductEntity" />

    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/_8sdp"
        android:paddingBottom="@dimen/_16sdp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="@dimen/_90sdp"
            android:scaleType="fitXY"
            app:networkImage="@{product.firstImage}"
            tools:ignore="ContentDescription"
            tools:srcCompat="@drawable/gym_category_for_test" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/_8sdp"
            android:text='@{product.name,default="product name"}'
            android:textColor="@color/black" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:scrollHorizontally="true"
            android:text='@{product.desc,default="product desc"}'
            android:textColor="@color/grey" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text='@{product.regularPrice+" "+product.settingCurrency,default="price"}'
            android:textColor="@color/black"
            app:isVisible="@{product.onSale}"
            tools:ignore="HardcodedText" />
    </LinearLayout>
</layout>